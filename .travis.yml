language: objective-c

branches:
 only:
 - master

xcode_workspace: Jolt.xcworkspace
xcode_scheme: Jolt
osx_image: xcode7
xcode_sdk: iphonesimulator9.0

before_install:
  - pip install codecov
after_success:
  - codecov
